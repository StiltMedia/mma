<p id="notice"><%= notice %></p>

<h1>Listing Thinktanks</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>User</th>
      <th>Thinktank</th>
      <th>Picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @thinktanks.each do |thinktank| %>
      <tr>
        <td><%= thinktank.title %></td>
        <td><%= thinktank.user %></td>
        <td><%= thinktank.thinktank %></td>
        <!--<td><%= thinktank.picture %></td>-->
        <td><%= link_to 'Show', thinktank %></td>
        <td><%= link_to 'Edit', edit_thinktank_path(thinktank) %></td>
        <td><%= link_to 'Destroy', thinktank, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Thinktank', new_thinktank_path %>
